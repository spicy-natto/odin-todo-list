import Task from "../src/modules/Task";
import util from "../src/modules/utilities";
import testUtil from "../src/modules/testUtilities";

describe("filterTaskArray", () => {
  test("filters Task array based on 'project' filter", () => {
    expect(true).toBe(false);
  });
  test("filters Task array based on 'dateGreaterThan' filter object", () => {
    expect(true).toBe(false);
  });
  test("filters Task array based on 'dateEquals' filter object", () => {
    expect(true).toBe(false);
  });
  test("filters Task array based on 'all' filter object", () => {
    expect(true).toBe(false);
  });
  test("filters Task array based on 'all' if filterType of filter object is not recognized", () => {
    expect(true).toBe(false);
  });
});

describe("createTaskViewData", () => {
  test("dummy test", () => {
    expect(true).toBe(false);
  });
});

describe("createTitleData", () => {
  test("dummy test", () => {
    expect(true).toBe(false);
  });
});

describe("filterProjectsByTaskArray", () => {
  test("dummy test", () => {
    expect(true).toBe(false);
  });
});

describe("getSelectedSidebarItem", () => {
  test("dummy test", () => {
    expect(true).toBe(false);
  });
});
